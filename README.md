# EventPass

EventPass is a local event discovery and ticket booking platform.

## Current setup

- Django project: `config`
- Apps: `accounts`, `events`, `tickets`, `bookings`, `payments`
- Database: SQLite for local development
- Admin route: `/admin/`

## Run locally

```bash
source .venv/bin/activate
python manage.py runserver
```
